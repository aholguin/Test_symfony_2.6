
    <div class="">
        <nav class="navbar navbar-default" role="navigation">
            <!-- El logotipo y el icono que despliega el menú se agrupan
                 para mostrarlos mejor en los dispositivos móviles -->
            <div class="navbar-header">

                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                    <span class="sr-only">Desplegar navegación</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="home">EW</a>
            </div>
            <p class="navbar-text navbar-right" style="margin-right: 10px; margin-left: 10px;">Anderson Holguin  
                <a href="logout" class="navbar-link">(Salir)</a></p>
            <div class="collapse navbar-collapse navbar-ex1-collapse">
                <ul class="nav navbar-nav">
                    {% for item in menu %}
                        {% if item.hijos is defined %}
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                    {{item.modulo.nombre}} <b class="caret"></b>
                                </a>
                                <ul class="dropdown-menu">
                                    {% for submenu in item.hijos %}
                                        <li class="divider"></li>
                                        <li><a href="{{ submenu.modulo.ruta }}">{{ submenu.modulo.nombre }}</a></li>
                                        {% endfor %}   

                                </ul>
                            </li>
                        {% else %}
                            <li><a href="{{ item.modulo.ruta }}">{{ item.modulo.nombre }}</a></li>
                            {% endif %}
                        {% endfor %}
                </ul>
            </div>
        </nav>   
    </div>
